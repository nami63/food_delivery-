import 'package:flutter/cupertino.dart';

class MyDecrpion extends StatelessWidget {
  const MyDecrpion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
